<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Compose owns the real theming (ui/theme/Theme.kt); this only styles the window
         before the first frame. -->
    <style name="Theme.Kamerplanter" parent="android:Theme.Material.Light.NoActionBar" />
</resources>
