<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- No CAMERA permission: USB host access is governed by the per-device USB grant,
         and nothing here touches the platform camera framework. The previous engine
         refused to open without it, but that was AUSBC's own check, not the platform's. -->

</manifest>
